#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int tc;
    cin >> tc;
    vector <int> res (tc,0);

    for(int j = 0; j < tc; j++)
    {
    int t; int operation = 0;
    cin >> t;
    vector <int> initial (t,0);
    vector <int> target (t);

    for ( int i = 0; i < t; i++){
        cin >> target[i];
    }

    }
   

    return 0;
}